databases:
  - name: khaled
    databaseName: khaled_qs12
    user: khaled_qs12_user
    plan: free

services:
  - type: web
    name: khaled
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec puma -c config/puma.rb"
    envVars:
      - key: postgres://khaled_qs12_user:ntawYvAxKUIsEenbkKA51Ma8y9TbzLMq@dpg-coion9n79t8c738ksfi0-a/khaled_qs12
        fromDatabase:
          name: khaled
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default
